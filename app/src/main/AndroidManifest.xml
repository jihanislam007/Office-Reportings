<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="md.mazharul.islam.jihan.reportings">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LogInActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReporterActivity" />
        <activity
            android:name=".AdminViewActivity"
            android:label="@string/title_activity_admin_view"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AdminNewsViewActivity"
            android:label="@string/title_activity_admin_news_view"
            android:theme="@style/AppTheme" />
        <activity android:name=".GenarelMessageSendActivity" />
        <activity android:name=".NewAcountActivity" />
        <activity android:name=".ReporterListActivity" />
        <activity android:name=".CentralMessageGetActivity"></activity>
    </application>

</manifest>